apiVersion: v1
kind: Service
metadata:
  name: taskmanager-app
  labels:
    app: taskmanager-app
spec:
  selector:
    app: taskmanager-app
  ports:
    - name: http
      port: 8080
      targetPort: http
  type: NodePort
